fix_plates : <BR>searchDB : <BR>update_sequence : Updates the sequence SQL database by reading data that was mirrored from<BR>decompress : <BR>setDB : This program sets up a Table documenting Field parameters for a database.<BR>add_aliases : <BR>test_Sequencing_API : <BR>backup_RDB : This program backup the MySQL database with replication implemented<BR>Notification : This program drives various notification messages.<BR>upgrade_DB : <BR>daily_upgrade : This program:<BR>restore_RDB : This program restore the MySQL database with replication implemented<BR>complement : This program generates ACGT complement strings (for files or STDIO)<BR>check_links : <BR>fasta : <BR>cluster_update_sequence : <BR>code_review : Allow review of code changes committed to CVS and tide to Issue ID<BR>phredscores : <BR>check_run_links : This program checks for the presence of trace files for each sequence run <BR>backup_DB : <BR>check_hierarchy : <BR>build_pod : <BR>parse_table : <BR>trace_archive : This program checks for the presence of trace files for each sequence run <BR>quick_restore : <BR>test : <BR>cleanup_mirror : <BR>DB_copy : <BR>genss : This program generates Sample Sheets for Sequencing Runs (and updates database)<BR>update_library_list : <BR>update_Stats : <BR>cleanup_web : <BR>get_warnings : <BR>restore_DB : <BR>usage_growth : <BR>cleanup : This program:<BR>quick_view : <BR>cat_vectors : This program regenerates a file with all vectors (and ecoli) to be screened<BR>check_replication : This allows monitoring of replication database... <BR>trace_bundle : <BR>parse_pool_data : <BR>test2 : <BR>usage : This is designed to monitor disk usage to ensure that space limitations are <BR>DB_monitor : Performs various database monitoring functions<BR>convert_DB : <BR>check_logs : Search log files for specific words/phrases and spit out results.<BR>sys_monitor : Performs various system resources monitoring<BR>transfer : <BR>